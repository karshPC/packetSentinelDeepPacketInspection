# Deep Packet Inspection

A C++17 deep packet inspection system for analyzing and filtering network traffic from PCAP files.

## Project Structure

- `include/` — C++ header files
- `src/` — C++ source files
- `tests/` — test programs and test data